@model List<task_distribution_app.Models.ViewModels.TaskVM>
@{
    ViewBag.Title = "Analist Paneli";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="row mt-3">
    <div class="col-12">
        <div class="card border-0 shadow-sm">
            <div class="card-body text-bg-light">
                <div class="row">
                    <div class="col-12 d-flex justify-content-between">
                        <h5>Task Listesi</h5>
                        <div>
                            <a href="/analyst/task/create" class="btn btn-primary btn-sm"><i class="bi bi-plus-lg"></i></a>
                        </div>
                    </div>
                </div>
                <hr />
                @if (ViewBag.error != null)
                {
                    <div class="alert alert-danger text-center">
                        <span>HATA: @ViewBag.Error</span>
                    </div>
                }
                <div class="row">
                    <div class="col-12">
                        <div class="table-responsive rounded-sm">
                            <table class="table table-sm table-hover">
                                <thead>
                                    <tr>
                                        <th>Başlık</th>
                                        <th>Tarihi</th>
                                        <th>Analist</th>
                                        <th>Developer</th>
                                        <th>Tamamlandı</th>
                                        <th class="text-end">İşlemler</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (task_distribution_app.Models.ViewModels.TaskVM task in Model)
                                    {
                                        <tr>
                                            <td>@task.title</td>
                                            <td>@(((DateTime)task.createdAt).ToString("dd.MM.yyyy"))</td>
                                            <td>@task.createdUserFullname</td>
                                            <td>@task.assignedUserFullname</td>
                                            <td>@(task.isComplete == true ? "Evet" : "Hayır")</td>
                                            <td class="text-end">
                                                <a href="/analyst/task/@task.id/details" class="btn btn-info btn-sm me-1"><i class="bi bi-search"></i></a>
                                                <a href="/analyst/task/@task.id/edit" class="btn btn-warning btn-sm me-1"><i class="bi bi-pencil"></i></a>
                                                <a href="/analyst/task/@task.id/delete" class="btn btn-danger btn-sm"><i class="bi bi-trash3"></i></a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>



